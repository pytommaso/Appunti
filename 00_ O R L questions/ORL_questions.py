# fortune 500 is s&p 500 ?

********************** Python **********************

    #1.
    #2.
    #3. non riesco a pip install itertools!
    #4.
    #5. ok le classi, ma che cazzo ci capisci?
        https://matplotlib.org/3.1.0/api/_as_gen/matplotlib.axes.Axes.set_xticks.html#matplotlib.axes.Axes.set_xticks

    #6. usi il code jumping, ammesso che esista...
    #6. ci sono cose come symbols-tree-view, ma non è meglio appena creo
        #codice riusabile esternalizzarlo in altro file?
    #6.
    #6. why print(np.linspace(0, 7, 8)) = [0. 1. 2. 3. 4. 5. 6. 7.] coi puntini?
    #6. Python è opensource? Ma io posso modificare python?
        #(Es stupido "definisci_funzione" al posto di def)
    #6. non esiste l'opposto di =   , vero?
    #6. perchè Equivalent to: e non it is !!!
        https://docs.python.org/3/library/functions.html#enumerate



    #6. pesa di più un csv '1000.00' o uno '1000000' ?
    #6. Return sends a specified value back to its caller whereas Yield can produce a sequence of values.
        #We should use yield when we want to iterate over a sequence, but don’t want to store the entire sequence in memory.
        #"don’t want to store the entire sequence in memory" ?

    #7. Scusa, ma che cazzo vuol dire?
        https://www.learnpython.org/en/Loops
        #For loops can iterate over a sequence of numbers using the "range" and "xrange" functions.
        #The difference between range and xrange is that the range function returns a new list with
        #numbers of that specified range, whereas xrange returns an iterator, which is more efficient.
        #(Python 3 uses the range function, which acts like xrange). Note that the range function is zero based.

    #8. liste = mutabili*
        #    * e si mutano da dentro fuori una def nello stesso modo:
        #        cambia il contenuto, l'id rimane lo stesso!
        #
        #con global
        #stringhe diventano "mutabili", ma in realtà - come se lo fai fuori dalla funzione-
        #si crea una nuova stringa con lo stesso nome (col nuovo contenuto) e con un nuovo id



********************** Atom **********************

    #1. vedere in realtime exception e/o
        #syntax errors sulla singola riga (centra col Linter?)
    #2. CAZZO GLI ARGOMENTI/METODI/CLASSI/TUTTO CIò CHE è POSSIBILE:
        #a volte sembra che non suggerisca tutte le possibilità

    #3. HIGHLIGHT:  delle stesse parole ok
                    #delle stesse variabili ??
                    #arbitrario e permanente ??
                    #TOP: boxes/rectangle around syntax blocks
    #4. questo addirittura evidenzia i blocchi di codice
        https://stackoverflow.com/questions/43225916/atom-editor-1-15-0-with-go-plus-has-red-background-in-function-source-code
    #5. usi Hydrogen?
    #5. multi-font syntax?
    #6. FAS frequent atom shortcut
    #6. impaginazione  (ex.: '=' + 'spacebar' = ' = ' )
    #6. put some flags on the code (disegna dei punti di riferimento)
    #6. disabilitare auto-foding
    #6. blocca/sposta la console
    #6. voglio 2 console: una global, una local ('cursor-oriented')
    #6. come gestisci la history/milestone...'ex: fin qui funzionava' (git?)
    #6. a volte si desincronizza con le 'cartelle di progetto'...con Git/github
        #funziona meglio?

    #x. delete/select cursor line
    #x. Deprecated...
    #x. atom-lupa... mai sentito parlare?
    #x.
    #x.
    #
